export const metadata = {
  title: "Data Member",
};

function LayoutMember({ children }: { children: React.ReactNode }) {
  return <div className="px-10 py-10">{children}</div>;
}

export default LayoutMember;
